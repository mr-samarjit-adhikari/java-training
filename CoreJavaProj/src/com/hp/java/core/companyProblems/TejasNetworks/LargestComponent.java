package com.hp.java.core.companyProblems.TejasNetworks;

/**
 * Largest Component
 * You are given a grid of N rows and M columns. Each cell of the grid contains a positive integer.
 * Now, you call two adjacent cells connected if the greatest common divisor of their values is exactly .
 * Also, you are given a choice to change at-most 1 integer in the grid. Your goal is to maximize the largest region of the grid that is connected.
 * In the output, print the maximum size of connected region possible after changing at most one element in the whole grid.
 *
 * Note: Two cells are considered to be in the same region if there is a path from one cell to the another one such that
 * all the adjacent cell values that occur in the path have greatest common divisor as .
 *
 * Input
 * The first line contains two space-separated integers N and M as input. Next N lines contain M integers each denoting elements of the grid.
 *
 * Output
 * In the output, you need to print the maximum size of the connected region that satisfies the condition above.
 *
 * Constraints
 * 1<= N,M <= 500
 * 1<= val <= 10 pow 6
 *  where val denotes the element of the grid.
 */

public class LargestComponent {
}
